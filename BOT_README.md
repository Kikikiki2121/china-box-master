# Telegram Bot для China Sky Logistics

Простой Telegram-бот на aiogram 3.x для обработки заказов из Web App калькулятора доставки.

## 📋 Возможности

- ✅ Приветственное сообщение с кнопкой Web App
- ✅ Получение адреса склада в Гуанчжоу
- ✅ Прием и обработка заказов из Web App
- ✅ Уведомления администратора о новых заказах

## 🚀 Установка и настройка

### 1. Установите Python

Убедитесь, что у вас установлен Python 3.8 или выше:
```bash
python3 --version
```

### 2. Установите зависимости

```bash
pip3 install -r requirements.txt
```

### 3. Создайте бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и придумайте имя и username для бота
4. Скопируйте токен, который даст вам BotFather

### 4. Получите свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш ID (это будет число, например: 123456789)

### 5. Настройте бота

Откройте файл `bot.py` и замените следующие значения:

```python
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"  # Вставьте токен от BotFather
ADMIN_ID = 12345  # Вставьте ваш Telegram ID
WEB_APP_URL = "YOUR_WEB_APP_URL_HERE"  # Вставьте URL вашего Web App
```

**Важно!** URL для Web App должен использовать HTTPS (кроме localhost для тестирования).

### 6. Настройте Web App в BotFather (опционально)

Для корректной работы Web App настройте домен в BotFather:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Нажмите "Bot Settings" → "Menu Button"
5. Отправьте URL вашего Web App

## ▶️ Запуск бота

```bash
python3 bot.py
```

Если все настроено правильно, вы увидите сообщение:
```
INFO - Бот запущен!
```

## 📱 Использование

1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start`
3. Нажмите на кнопку "🌐 Открыть калькулятор доставки"
4. Используйте калькулятор для расчета стоимости доставки

## 🔧 Интеграция с Web App

Ваш Web App должен отправлять данные боту в следующем формате:

### Запрос адреса склада:
```javascript
window.Telegram.WebApp.sendData(JSON.stringify({
    action: "get_address"
}));
```

### Отправка заказа:
```javascript
window.Telegram.WebApp.sendData(JSON.stringify({
    action: "order",
    data: {
        weight: 100,        // вес в кг
        volume: 5,          // объем в м³
        deliveryType: "Авиа",
        price: 850          // цена в долларах
    }
}));
```

## 📦 Структура данных

### Action: get_address
Возвращает адрес склада в Гуанчжоу.

### Action: order
Принимает данные заказа и:
1. Отправляет подтверждение пользователю: "✅ Расчет принят. Менеджер скоро напишет."
2. Отправляет уведомление администратору с деталями заказа

## ⚠️ Важные замечания

- Не публикуйте токен бота в открытых репозиториях
- Для продакшена рекомендуется использовать переменные окружения для хранения токенов
- URL Web App должен быть доступен по HTTPS
- Убедитесь, что ваш сервер доступен из интернета для работы с Telegram API

## 🐛 Отладка

Бот логирует все события в консоль. Если что-то не работает:

1. Проверьте, что токен бота верный
2. Убедитесь, что ADMIN_ID - это ваш корректный Telegram ID
3. Проверьте, что WEB_APP_URL доступен и использует HTTPS
4. Посмотрите логи в консоли для выявления ошибок

## 📝 Примеры использования

### Остановка бота
Нажмите `Ctrl+C` в терминале

### Запуск в фоновом режиме (Linux/Mac)
```bash
nohup python3 bot.py > bot.log 2>&1 &
```

### Просмотр логов в реальном времени
```bash
tail -f bot.log
```

## 🆘 Поддержка

Если у вас возникли проблемы:
1. Проверьте документацию [aiogram](https://docs.aiogram.dev/)
2. Убедитесь, что все зависимости установлены
3. Проверьте логи бота на наличие ошибок
